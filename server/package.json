{
  "name": "messaging-app",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "scripts": {
    "build": "bun build",
    "test": "bun test",
    "dev": "bun run --watch index.ts"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}